{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start:core": "lerna exec --scope core -- yarn start:dev",
    "start:store": "lerna exec --scope store -- yarn dev",
    "start:store-admin": "lerna exec --scope store-admin -- yarn run dev",
    "start:store-detail": "lerna exec --scope store-detail -- yarn run dev",
    "start:store-payment": "lerna exec --scope store-payment -- yarn run dev"
  },
  "devDependencies": {
    "craco": "^0.0.3",
    "lerna": "^6.4.1"
  }
}
